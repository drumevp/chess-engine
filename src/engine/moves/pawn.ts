import { type Move } from "../types/main";
import type { MoveGenerationContext } from "./types";

const generatePawnMoves = (_ctx: MoveGenerationContext): Move[] => {
  // TODO:

  return [];
}

export default generatePawnMoves;